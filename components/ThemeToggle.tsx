import React from 'react';
import { View } from 'react-native';

// Theme toggle is disabled - always use dark theme with brand colors
// Integrated with repo2's existing color system
export function ThemeToggle() {
  return <View />;
}