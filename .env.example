# ============================================
# Database Configuration
# ============================================

# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Example: postgresql://heinicus_user:your_password@localhost:5432/heinicus_db
DATABASE_URL="postgresql://heinicus_user:your_password@localhost:5432/heinicus_db"

# ============================================
# JWT Authentication
# ============================================

# Secret key for JWT token signing and verification
# IMPORTANT: Generate a secure random string (min 64 characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="your-very-long-and-secure-secret-key-at-least-64-characters-long"

# ============================================
# Frontend Configuration
# ============================================

# Frontend URL for password reset links and CORS
# Development: http://localhost:8081
# Production: https://yourdomain.com
FRONTEND_URL="http://localhost:8081"

# Additional CORS origins (comma-separated)
# Example: https://admin.heinicus.app,https://api.heinicus.app
ADDITIONAL_CORS_ORIGINS=""

# ============================================
# WebSocket Configuration (Phase 2)
# ============================================

# WebSocket server port
# Default: 3001
WEBSOCKET_PORT="3001"

# WebSocket URL for frontend clients
# Development: http://localhost:3001
# Production: wss://ws.yourdomain.com
EXPO_PUBLIC_WEBSOCKET_URL="http://localhost:3001"

# ============================================
# Email Configuration (SMTP)
# ============================================

# SMTP server configuration for sending password reset emails
# Gmail example shown below

# SMTP host
SMTP_HOST="smtp.gmail.com"

# SMTP port (587 for TLS, 465 for SSL, 25 for unencrypted)
SMTP_PORT="587"

# Use secure connection (true for SSL, false for TLS)
SMTP_SECURE="false"

# SMTP authentication credentials
# For Gmail: Use app-specific password (not your regular password)
# Generate at: https://myaccount.google.com/apppasswords
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-specific-password"

# From address for outgoing emails
SMTP_FROM="noreply@heinicus-mobile-mechanic.app"

# ============================================
# SMS Configuration (Optional - Twilio)
# ============================================

# Twilio credentials for SMS-based 2FA (future feature)
# Sign up at: https://www.twilio.com/
# TWILIO_ACCOUNT_SID="your-twilio-account-sid"
# TWILIO_AUTH_TOKEN="your-twilio-auth-token"
# TWILIO_PHONE_NUMBER="+1234567890"

# ============================================
# Application Environment
# ============================================

# Node environment (development, production, test)
NODE_ENV="development"

# ============================================
# Security Notes
# ============================================

# IMPORTANT SECURITY GUIDELINES:
# 1. NEVER commit this file with real credentials to version control
# 2. Keep .env in .gitignore (already configured)
# 3. Generate strong, unique passwords for production
# 4. Rotate JWT_SECRET periodically in production
# 5. Use environment-specific credentials (dev/staging/production)
# 6. Store production secrets in secure vault (AWS Secrets Manager, etc.)
# 7. Enable 2FA for all service accounts (Gmail, Twilio, etc.)
# 8. Use different database credentials per environment
# 9. Monitor for unauthorized access attempts
# 10. Keep this file permissions restricted (chmod 600 .env)

# ============================================
# Quick Start
# ============================================

# 1. Copy this file:
#    cp .env.example .env
#
# 2. Update all placeholder values with your actual credentials
#
# 3. Generate JWT secret:
#    node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#
# 4. For Gmail SMTP:
#    - Enable 2FA on your Google account
#    - Generate app-specific password
#    - Use that password in SMTP_PASS
#
# 5. Test your configuration:
#    npm run test:security
