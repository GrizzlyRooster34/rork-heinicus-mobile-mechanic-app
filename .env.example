# Expo Configuration Environment Variables
# Copy this file to .env and fill in your actual values

# Google Services JSON file path (for Firebase/Google Maps on Android)
GOOGLE_SERVICES_JSON=./google-services.json

# Google Maps API Key (for both iOS and Android)
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# EAS Project ID (from Expo Application Services)
EAS_PROJECT_ID=your_eas_project_id_here

# Backend API URL (accessible in app via expo-constants)
# Use EXPO_PUBLIC_ prefix to make variables available in the app at runtime
EXPO_PUBLIC_API_URL=http://localhost:3000

# Environment (development, staging, production)
EXPO_PUBLIC_ENVIRONMENT=development

# Additional environment-specific variables can be added here
# All EXPO_PUBLIC_* variables are accessible in the app via:
# import Constants from 'expo-constants';
# const apiUrl = Constants.expoConfig.extra.apiUrl;
